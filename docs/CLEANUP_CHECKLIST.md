# リリース前クリーンアップチェック
- 旧 `.env` / `.env.*`（秘密直書き）を削除し `.gitignore` 済みか
- 旧起動スクリプト・未使用 `.cmd/.sh` を削除（上記「RUN_*」に統一）
- 未使用の画像・旧アイコンを削除（正式採用の3Dアイコン・女性アバターのみ）
- `phase2_docs` 以外の重複文書・旧版 README を削除/ 統合
- RELEASE_NOTES / NEXT_STEPS を最新版へ更新
